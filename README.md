# Gemora (Vercel-ready, PWA, Paystack, Cloudinary, Neon)
Deploy on Vercel → add env vars from `.env.example` → Deploy.

Admin: `/admin-login` → `admin@gemora.com` / `Gemora@2025`
Webhook: set in Paystack → `/api/paystack/webhook`
PWA: included manifest + service worker.
